# PaperAgent V2 · P0 阶段交付文档

> 阶段：P0 可信基础（M1 前置）
> 完成日期：2026-09-24
> 验收结论：✅ 通过

---

## 一、阶段目标

P0 是 V2 改造的第一阶段，目标是把「可追溯 Evidence 数据结构」打通，并修复改造前存在的 9 项 P0 级工程问题，为后续 Hybrid Retrieval、Decision Engine、Research Mode 建立底座。

核心原则：**先建立可追溯 Evidence，再升级检索与结构化决策**；检索/回答结果全程保持结构化对象，不转字符串后再解析。

---

## 二、完成的工作

### 2.1 新增模块

| 文件 | 说明 |
|------|------|
| `PaperQuery_Backend/core/common/types.py` | 公共类型：`EvidenceChunk` / `Citation` / `AnswerPackage` |
| `PaperQuery_Backend/core/common/errors.py` | 统一异常类型（`PaperAgentError` 及子类） |
| `PaperQuery_Backend/core/common/config.py` | `Settings` 集中配置（从环境变量读取，带默认值） |
| `PaperQuery_Backend/core/utils/chunking.py` | `TextChunk` + `split_text`（字符窗口 1800 + overlap 250） |
| `PaperQuery_Backend/core/evidence/formatter.py` | 证据格式化 + Citation 映射（`format_evidence` / `build_citations`） |

### 2.2 九项 P0 工程修复

| # | 问题 | 修复动作 | 状态 |
|---|------|---------|------|
| 1 | 检索结果被字符串化 | `chromadb.py` 新增 `search_evidence()` 返回 `List[EvidenceChunk]`（含 document_id/page_number/score） | ✅ |
| 2 | Judge 失败默认「相关」 | `chat_judge_relate` 异常时 `is_relevant=False`（Fail-closed） | ✅ |
| 3 | 固定 k=12 | 预留 `Settings.FINAL_K/DENSE_K/SPARSE_K` 等配置（动态 Top-K 属阶段二） | ✅ 预留 |
| 4 | 多文件聊天接口鉴权不完整 | `chat_multi_file_generate_flow` 增加 `Depends(oauth2_scheme)` | ✅ |
| 5 | 临时上传接口未鉴权 | `multi_file_chat_upload` 增加 token + `get_current_user` 校验 | ✅ |
| 6 | 前端直连模型服务 | 删除 `src/api/gpt.ts` | ✅ |
| 7 | CORS 过宽 | `main.py` 从 `Settings.FRONTEND_ORIGINS` 白名单读取 | ✅ |
| 8 | 无数据库迁移工具 | `alembic` 已列入依赖（迁移脚本属阶段八） | ⏳ 待阶段八 |
| 9 | Embedding 与方案不一致 | BGE-M3 模型已下载就位（索引迁移属阶段二） | ⏳ 待阶段二 |

### 2.3 依赖与配置更新

- `requirement.txt`：新增 `FlagEmbedding` / `rank-bm25` / `sentence-transformers` / `pydantic-settings` / `alembic` / `jieba`
- `.env.example`：新增 V2 Retrieval / Research / Security 配置段（INDEX_VERSION、BGE_M3_MODEL_PATH、DENSE_K、FRONTEND_ORIGINS 等）

### 2.4 模型下载

| 模型 | 关键文件 | 大小 | 状态 |
|------|---------|------|------|
| BGE-M3 | `pytorch_model.bin` + `colbert_linear.pt` + `sparse_linear.pt` + tokenizer | 3.35 GB | ✅ 完整 |
| bge-reranker-v2-m3 | `model.safetensors` | 2.14 GB | ✅ 完整 |

> 下载说明：BGE-M3 权重为 `pytorch_model.bin`（非 `.safetensors`）；HuggingFace xet CDN 在当前网络超时，改用 modelscope 直连下载完成。

---

## 三、文件清单

**新增（8 个）**

```
PaperQuery_Backend/core/common/__init__.py
PaperQuery_Backend/core/common/types.py
PaperQuery_Backend/core/common/errors.py
PaperQuery_Backend/core/common/config.py
PaperQuery_Backend/core/utils/chunking.py
PaperQuery_Backend/core/evidence/__init__.py
PaperQuery_Backend/core/evidence/formatter.py
PaperQuery_Backend/models/          # BGE-M3 + Reranker 模型缓存目录
```

**修改（8 个）**

```
PaperQuery_Backend/main.py                          # CORS 白名单 + 引入 Settings
PaperQuery_Backend/core/agent/chatAgent.py          # Fail-closed
PaperQuery_Backend/core/vectordb/chromadb.py        # search_evidence()
PaperQuery_Backend/core/backend/router/router_llm.py          # 多文件聊天鉴权
PaperQuery_Backend/core/backend/router/router_document.py     # 临时上传鉴权
PaperQuery_Backend/requirement.txt                  # V2 依赖
PaperQuery_Backend/.env.example                     # V2 配置项
PaperQuery_Frontend/src/api/gpt.ts                  # 已删除
```

---

## 四、验证结果

| 验证项 | 结果 |
|--------|------|
| 公共类型 import | ✅ 通过 |
| `split_text` 切块 | ✅ 通过 |
| `format_evidence` / `build_citations` | ✅ 通过（生成 [C1] 引用） |
| `Settings` 配置读取 | ✅ 通过 |
| `search_evidence()` 返回结构化 `EvidenceChunk` | ✅ 通过（含 document_id/page_number/dense_score） |
| 后端 `main.py` 启动 + CORS/鉴权 | ✅ 通过 |
| 后端 Python 语法检查 | ✅ 全部通过 |

---

## 五、进入阶段二的待办

1. 安装依赖：`torch`（约 2GB）+ `FlagEmbedding` + `sentence-transformers` + `rank-bm25`
2. 实现 `core/retrieval/`：
   - `dense.py`（BGE-M3 Dense Retriever）
   - `sparse.py`（BM25 Retriever）
   - `fusion.py`（RRF）
   - `reranker.py`（bge-reranker-v2-m3）
   - `pipeline.py`（Hybrid + Adaptive Top-K）
3. `scripts/reindex_v2.py`：用 BGE-M3 重建 V2 索引到 `CHROMA_LAYER1_V2_DIR`
4. 验证模型实际加载与检索

---

## 六、回滚点

- P0 仅做增量改动，未破坏既有 `/chat/*` 与 `/document/*` 接口；
- 旧 ONNX embedding 与旧 Chroma layer1 仍保留，可随时切回 Basic RAG baseline；
- BGE-M3/Reranker 为独立新增目录，不影响旧运行链路。
