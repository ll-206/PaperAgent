# PaperAgent V2 · 未完成任务清单

> 更新时间：2026-09-24
> 整体完成度：约 60%（功能主干 78% / 实验验证 ~20% / 交付材料 0%）
> 说明：本清单基于对项目源码的重新审计与豆包评估结果交叉核对后整理，状态列均已与代码核实。

---

## 一、总览

| 板块 | 完成度 | 结论 |
|------|--------|------|
| 功能主干（检索/决策/Research/前端） | ~78% | 可跑，细节待落地 |
| 实验验证（三组实验） | ~20% | 实验一框架完成，实验二/三空白 |
| 比赛交付材料（A1–A4/视频/PPT） | 0% | 全部未产出 |

**一句话总结**：功能主干基本跑通，但「能证明它有效」的实验、两个创新点（Quantum/Experiment）落地、以及全部对外交付材料这三块是当前的硬缺口。

---

## 二、未完成事项明细

### ① 实验验证（优先级：高）

| 事项 | 当前状态 | 说明 |
|------|---------|------|
| 实验一 · 可信问答框架 | ✅ 已完成 | 48 条种子 + 3 脚本（build/run/score）+ 3 配置 + 冒烟通过 |
| 实验一 · 扩到 200 题 / 30 篇 | ❌ 未做 | 库仅 1 篇 PDF，需补论文 → 重建 V2 索引 → `build_qa_dataset.py --expand 200` → 重跑 |
| 实验一 · 全量跑批出正式结论 | ❌ 未做 | 目前仅 3+4 条冒烟，无正式指标 |
| 实验二 · research_30（Task Completion） | ❌ 未做 | 数据集、脚本、结果均缺 |
| 实验三 · quantum_20（Quantum 评测） | ❌ 未做 | 且被 Quantum 真实调用卡死（见 ②） |

### ② 功能缺口（优先级：中/高）

| 事项 | 当前状态 | 说明 / 涉及文件 |
|------|---------|----------------|
| Planner 计划质量 | ❌ 有 bug | 实测 `citation_verify` 步骤 input 为空 → 任务 FAILED；需优化 PLANNER_PROMPT 的步骤依赖与输入 |
| skill_runs 表写入 | ❌ 未写 | 表结构已就绪，Executor 从未记录每次 Skill 调用审计（run_id/duration_ms/trace_json） |
| Citation 点击跳页（页码定位） | ⚠️ 部分完成 | 前端已渲染可点击 `[C#]` 并跳转 `pdfInfo`；但 `pdfInfo.vue`/`pdfViewer.vue` 未接收 `query.page`，未定位到具体页码 |
| DecisionBadge | ❌ 未做 | 前端未消费 SSE `decision` 事件（证据不足/扩检提示） |
| StepTimeline 实时态 | ⚠️ 部分完成 | 耗时已展示；缺 PENDING/RUNNING 中间态（后端同步执行所致） |
| Adaptive Top-K 自动扩检 | ❌ 未实现 | 产品链路未接（仅 eval 脚本里有 expand 逻辑） |
| Alembic 迁移 | ❌ 未引入 | 仍靠 `Base.metadata.create_all` 建表 |
| Quantum 真实调用 | ❌ 骨架 | adapter 仅骨架、白名单空，缺真实 CLI/API |
| Experiment 前端模式 | ❌ 占位 | ModeSwitch 中 Experiment 仅提示未实现 |
| evidence_records / chat_sessions 表 | ❌ 未建 | 证据追踪与会话持久化缺失 |
| Executor 并行 | ❌ 顺序 | 未做互不依赖步骤并行（文档标注首版可顺序） |
| Grounding 首 token 延迟 | ❌ 未优化 | 先生成后校验，流式体验差 |
| dense_score 口径 | ❌ 未改 | 存 L2 距离非相似度（不影响 RRF 排名） |
| 依赖 freeze | ❌ 未锁 | `requirement.txt` 未 pin 精确版本 |
| Chat 页走 `/qa/stream` | ✅ 已接通 | `messageList.ts` 调用 `@/api/qa` 的 `askQuestionStream`，已走 V2 `/qa/stream` |

### ③ 比赛交付材料（优先级：高，但依赖实验先跑通）

| 材料 | 状态 |
|------|------|
| 附件 A1（需求/技术细节图） | ❌ 未产出 |
| 附件 A2（创新性说明） | ❌ 未产出 |
| 附件 A3（实验/测试截图） | ❌ 未产出（依赖实验一正式跑通） |
| 附件 A4（开源/复现说明） | ❌ 未产出 |
| 演示视频 | ❌ 未产出 |
| 答辩 PPT | ❌ 未产出 |

---

## 三、建议推进顺序

1. **实验一扩规模 + 全量跑通**（补论文 → 重建索引 → `--expand 200` → 全量 run + score → 出正式指标）
2. **修 Planner / 写 skill_runs**（两个高优先级功能缺口）
3. **实验二 research_30**（补数据集 + 脚本 + 结果）
4. **Quantum 真实调用 → 实验三**（拿到真实 CLI/API 后补 adapter + 白名单）
5. **Citation 页码定位 + DecisionBadge**（前端体验收尾）
6. **最后集中产出 A1–A4 / 视频 / PPT**

> 第一步「实验一扩规模」同时是技术方案「已实现」的立论依据，并可直接喂给附件 A3，建议优先。
