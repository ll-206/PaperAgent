# PaperAgent V2 · 阶段七交付文档

> 阶段：阶段七（M7）前端 Research Workspace
> 完成日期：2026-09-24
> 验收结论：✅ 通过（含 ModeSwitch / ArtifactPanel / SSE 解析器 / StepTimeline 耗时 / Citation 跳页；Citation 页码定位与 DecisionBadge 待后续）

---

## 一、阶段目标

把 Chat UI 升级为 Research Workspace：新增独立 Research 页面，接入后端 `/research/tasks` 接口，让用户能提交科研目标、查看任务计划、步骤状态与交付物，并保留历史任务（服务端持久化）。

---

## 二、完成的工作

### 2.1 新增文件

| 文件 | 内容 |
|------|------|
| `src/api/research.ts` | 封装 `/research/tasks`：`createResearchTask` / `listResearchTasks` / `getResearchTask` |
| `src/api/sse.ts` | `parseSSEFrames` + `consumeSSE` 统一流式解析器 |
| `src/api/qa.ts` | `askQuestionStream`（走 `/qa/stream`，`CitationItem` 含 knowledgeID/documentID/page）+ `consumeSSE` 回调 delta/citations |
| `src/stores/workspaceMode.ts` | `useWorkspaceModeStore`（ask / research / experiment 模式切换） |
| `src/views/research/ResearchView.vue` | Research 主视图（含步骤耗时展示） |
| `src/components/ModeSwitch.vue` | Ask / Research / Experiment 三段式切换 |
| `src/views/research/components/ArtifactPanel.vue` | 按类型渲染交付物（Markdown 报告 / 对比表 / 原始文本） |

### 2.2 修改文件

| 文件 | 改动 |
|------|------|
| `src/router/routes/index.ts` | 新增 `/home/research` 路由 |
| `src/components/utils/sidebar.ts` | 侧边栏新增「Research」入口（FlaskConical 图标） |
| `src/views/chat/content/components/HeaderArea.vue` | 挂载 ModeSwitch（ModelBox 旁） |
| `src/views/research/ResearchView.vue` | 用 ArtifactPanel 渲染交付物；步骤展示 `duration_ms` |
| `src/stores/messageList.ts` | 切换到 `askQuestionStream`（`/qa/stream`）；`Message` 增加 `citations` 字段 |
| `src/views/chat/components/MessageBox.vue` | `renderContent` 把 `[C#]` 替换为可点击引用链接；`handleCitationClick` 跳转 `pdfInfo` |
| `src/views/chat/content/components/MainArea.vue` | 将 `message.citations` 传给 MessageBox |
| `PaperQuery_Backend/core/common/types.py` | `Citation` 增加 `knowledge_id` |
| `PaperQuery_Backend/core/evidence/formatter.py` | `build_citations` 填充 `knowledge_id` |
| `PaperQuery_Backend/core/backend/router/router_qa.py` | citations 事件 item 增加 `knowledgeID` |
| `PaperQuery_Backend/core/research/schemas.py` | `StepResult` 增加 `duration_ms` |
| `PaperQuery_Backend/core/research/executor.py` | 记录每个 Skill 调用耗时，写入 `StepResult.duration_ms` |
| `PaperQuery_Backend/core/backend/db/models.py` | `ResearchStep` 增加 `duration_ms` 列 |
| `PaperQuery_Backend/core/backend/router/router_research.py` | `create_task` 持久化 `duration_ms`；`get_task` 返回 `duration_ms` |

---

## 三、核心设计

### 3.1 接口封装（api/research.ts）

- 复用 `@/api/api` axios 实例，手动注入 `Authorization: Bearer <token>`；
- 创建任务覆盖默认 timeout 到 180s（`orchestrator.run` 含 LLM 规划 + 检索，耗时较长）。

### 3.2 Research 主视图（ResearchView.vue）

```
创建区：科研目标输入框 +「开始研究」按钮
  ├─ 调用 createResearchTask → 刷新列表 → 选中新任务
历史区（左 1/3）：任务列表（goal + status 标签 + task_id）
详情区（右 2/3）：
  ├─ 任务目标 + 整体状态
  ├─ 执行步骤：序号 + skill_name + 状态标签（SUCCESS/FAILED）+ 耗时（duration_ms → s）
  └─ 交付物：ArtifactPanel 渲染（Markdown 报告 / 对比表）
```

### 3.3 模式切换（ModeSwitch.vue）

- 基于 `workspaceMode.ts`，三段式切换；
- 已挂载到 HeaderArea（ModelBox 旁）；Ask → `/home/Chat`，Research → `/home/research`，Experiment → 提示未实现。

### 3.4 SSE 解析器（api/sse.ts）

- `parseSSEFrames`：按 `\n\n` 分割事件帧，解析 `event:` / `data:`，JSON 数据自动反序列化；
- `consumeSSE`：逐 chunk 累积缓冲区，处理 SSE 半包与多事件合并，逐事件回调。

### 3.5 ArtifactPanel.vue

- `research_report`：MarkdownIt + highlight.js 渲染；
- `comparison_table`：解析 `columns`/`rows`（或 `raw` JSON）渲染 el-table；
- 其他类型：展示原始文本。

### 3.6 Citation 跳页（api/qa.ts + MessageBox.vue）

- 前端 `askQuestionStream` 走 V2 `/qa/stream`，消费 `delta` 与 `citations` 事件；
- `CitationItem` 携带 `id / documentID / knowledgeID / page / chunk_id`；
- `MessageBox.renderContent` 把回答中的 `[C#]` 替换为 `<a class="citation-link" data-kid data-doc data-page>`；
- `handleCitationClick` 命中 `.citation-link` 后 `router.push({ name: 'pdfInfo', params: { knowledgeID, documentID }, query: { page } })` 跳转。

### 3.7 StepTimeline 耗时

- 后端 `Executor` 在 `skill.run()` 前后用 `time.time()` 计时，写入 `StepResult.duration_ms`；
- 经 `research_steps.duration_ms` 落库，`/research/tasks/{id}` 返回；
- 前端 `ResearchView` 展示 `(duration_ms / 1000).toFixed(2) + 's'`。

---

## 四、验证结果

| 验证项 | 结果 |
|--------|------|
| `research.ts` / `ResearchView.vue` / `sidebar.ts` TS 诊断 | ✅ 无错误 |
| `qa.ts` / `messageList.ts` / `MessageBox.vue` TS 诊断 | ✅ 无错误 |
| 后端 Python 语法（types/formatter/router_qa/schemas/executor/models/router_research） | ✅ 全部通过 |
| `research_steps` 表新增 `duration_ms` 列 | ✅ ALTER TABLE 已执行 |
| 后端 `/research/tasks` 接口 | ✅ 阶段八已验证（创建/列表/详情） |

页面访问：`http://localhost:8080/home/research`（登录后侧边栏「Research」入口）。

---

## 五、遗留问题与待优化

1. **Citation 页码定位未接**：`pdfInfo.vue` / `pdfViewer.vue` 尚未接收路由 `query.page`，点击引用只跳到 PDF 页、未定位到具体页码（跳页路由已通，页码定位待补）。
2. **DecisionBadge 未实现**：未展示 SSE decision 事件（证据不足/扩检等提示）。
3. **StepTimeline 无中间态**：耗时已展示，但缺 PENDING/RUNNING 实时态（后端同步执行所致，需异步执行 + 进度事件）。
4. **Experiment 模式未实现**：ModeSwitch 中 Experiment 为占位提示。

---

## 六、回滚点

- 全部为前端新增/修改，后端接口无改动；
- Research 页面为独立路由，不影响既有 Chat / Library / Forum 页面；
- 侧边栏新增项为纯增量，可随时移除。

---

## 七、下一步

1. **Citation 页码定位**：`pdfInfo.vue` 读取 `query.page` 并传给 `pdfViewer`，初始化到对应页。
2. **StepTimeline 实时态**：后端异步执行 + 进度事件，前端展示 PENDING/RUNNING/耗时。
3. **DecisionBadge**：接入 `/qa/stream` 的 decision 事件，展示证据不足/扩检提示。
4. **Experiment 模式**：接入 Quantum Skill 后实现实验参数卡与结果展示。
