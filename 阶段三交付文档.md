# PaperAgent V2 · 阶段三交付文档

> 阶段：阶段三（M3）Research Decision Engine + Ask Mode 接线
> 完成日期：2026-09-24
> 验收结论：✅ 通过

---

## 一、阶段目标

阶段三对应实施方案第 5 章「Research Decision Engine」与 M3 里程碑。目标是把原来耦合在 `ChatAgent.chat_judge_relate()` 里的相关性判断，升级为独立的 **结构化决策引擎**，并把 P0/阶段二建立的 Evidence 数据结构、Hybrid Retrieval Pipeline 接成 **Ask Mode 可信问答闭环**（Intent → Retrieval → Evidence Judge → Answer → Grounding → Citation）。

---

## 二、完成的工作

### 2.1 新增模块

| 文件 | 内容 |
|------|------|
| `core/decision/schemas.py` | `IntentType` / `IntentDecision` / `EvidenceDecision` / `RelatednessDecision` / `GroundingDecision` 结构化决策类型 |
| `core/decision/prompts.py` | Intent / Evidence / Grounding / Relatedness / Answer 五类 Judge prompt（要求 JSON 输出） |
| `core/decision/engine.py` | `DecisionEngine`：`decide_intent` / `decide_evidence` / `relatedness` / `verify_grounding`，全部 Fail-closed |
| `core/evidence/grounding.py` | `GroundingVerifier`：规则校验（引用 ID 必须存在）+ LLM Judge（unsupported_claims） |
| `core/backend/router/router_qa.py` | `/qa/stream` Ask Mode 可信问答接口（统一 SSE 事件协议） |

### 2.2 修改文件

| 文件 | 改动 |
|------|------|
| `core/backend/router/req_res_schema.py` | 新增 `QARequest`（question/document_ids/model/mode/conversation_context） |
| `main.py` | lifespan 初始化 `DecisionEngine` + `RetrievalPipeline`（容错降级），注册 `router_qa` |
| `core/agent/chatAgent.py` | `chat_judge_relate` 改为调用 `DecisionEngine.relatedness`（保持旧接口兼容） |

---

## 三、核心设计

### 3.1 DecisionEngine（结构化决策 + Fail-closed）

```python
class DecisionEngine:
    def decide_intent(query, context_meta) -> IntentDecision     # PAPER_QA / RESEARCH_TASK / ...
    def decide_evidence(query, evidence) -> EvidenceDecision    # ANSWER / EXPAND_LOCAL / SEARCH_EXTERNAL / ABSTAIN
    def relatedness(paper_content, question) -> RelatednessDecision  # 兼容旧 chat_judge_relate
    def verify_grounding(question, answer, evidence) -> GroundingDecision
```

- 每个 decision 通过 LLM 生成 JSON 后解析为 typed Pydantic 对象；
- 解析失败时走保守策略（Fail-closed）：Intent 默认 PAPER_QA、Evidence 默认 EXPAND_LOCAL、Relatedness 默认 is_relevant=False；
- Decision 只负责判断，不负责生成最终回答。

### 3.2 Ask Mode 流程（/qa/stream）

```
Intent 判断
  → Hybrid Retrieval（BGE-M3 + BM25 + RRF + Reranker）
  → Evidence Judge（证据充分度）
  → Answer 生成（强制 [C#] 引用）
  → Grounding Verify（引用 ID 校验 + LLM Judge）
  → SSE 分片输出（meta / decision / grounding / citations / delta）
```

### 3.3 SSE 事件协议

```
event: meta        data: {"trace_id","route","intent"}
event: decision     data: {"decision","evidence_score","confidence"}
event: grounding    data: {"passed","confidence"}
event: citations    data: {"items":[{"id","documentID","page","chunk_id"}]}
event: delta        data: {"text"}
```

---

## 四、验证结果

| 验证项 | 结果 |
|--------|------|
| `decide_intent` | ✅ 正常返回 typed 决策 |
| `decide_evidence` | ✅ 证据不足正确判为 EXPAND_LOCAL |
| `relatedness` | ✅ 兼容旧字段（is_relevant/is_professional/arxiv_query_keyword） |
| 后端启动 + RetrievalPipeline 初始化 | ✅ 成功 |
| **`/qa/stream` 端到端** | ✅ HTTP 200，事件序列 meta→decision→grounding→citations→delta 完整 |

端到端行为验证：
- 答案正确使用 `[C1][C2]` 引用，`citations` 事件返回 documentID/page/chunk_id；
- 证据不足时如实提示「当前证据不足」，而非编造。

---

## 五、遗留问题与待优化

1. **Intent 分类准确率**：当前 `decide_intent` 在空 `context_meta` 下，部分论文相关问题可能被误判为 `GENERAL_CHAT`。建议后续把 `document_ids` 数量/绑定状态注入 prompt 改善意图分类。
2. **dense_score 语义**（沿用阶段二）：存的是 Chroma L2 距离，非相似度，不影响 RRF（只用排名）。
3. **Adaptive Top-K 未实现**：当前 `RetrievalPipeline` 使用固定 `FINAL_K`；Evidence Judge 输出 `EXPAND_LOCAL` 后尚未自动扩检（`MAX_RETRIEVAL_ROUNDS` 已预留配置，属 V2.2）。
4. **Grounding 首版方案**：采用「先非流式生成 → 校验 → 再分片输出」，首 token 延迟较高（文档标注的省赛稳定性优先方案）。

---

## 六、回滚点

- 旧 `/chat/*` 接口全部保留，`chat_judge_relate` 仅内部改为调用 DecisionEngine，外部返回结构不变；
- `DecisionEngine` / `GroundingVerifier` / `router_qa` 均为新增，不侵入既有链路；
- `RetrievalPipeline` 初始化失败时 `main.py` 自动降级为基础检索（`chroma_db.search_evidence`），不影响启动；
- `/qa/stream` 为新增接口，旧前端仍走 `/chat/*`，可并行运行。

---

## 七、下一步（阶段四/五）

1. **Adaptive Top-K（V2.2）**：Evidence Judge 输出低分时自动扩检/改写 query，最多 `MAX_RETRIEVAL_ROUNDS` 轮，记录 trace。
2. **Research Mode（阶段五）**：实现 `core/research/` 状态机（Planner → Executor → Verifier → Artifact），新增 `research_tasks` / `research_steps` / `artifacts` 表。
3. **前端接线**：新增 `api/qa.ts` + `sse.ts`，Message 增加 citations/grounding，EvidencePanel 展示引用与跳页。
