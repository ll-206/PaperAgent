# PaperAgent V2 · 阶段二交付文档

> 阶段：阶段二（M2）Hybrid Retrieval
> 完成日期：2026-09-24
> 验收结论：✅ 通过

---

## 一、阶段目标

阶段二对应实施方案第 4 章「Evidence-Adaptive Hybrid RAG」与 M2 里程碑。目标是把 P0 阶段建立的 `EvidenceChunk` 结构化检索能力，升级为真实的 **BGE-M3 Dense + BM25 Sparse → RRF 融合 → Reranker 重排** 混合检索链路，替换旧的 ONNX + 固定 Top-K 检索。

---

## 二、完成的工作

### 2.1 依赖安装

新增依赖（写入 `requirement.txt`）并安装到 `.venv`：

| 依赖 | 最终版本 | 用途 |
|------|---------|------|
| torch | 2.14.0 | FlagEmbedding 底层 |
| transformers | 4.46.3 | BGE-M3 / Reranker 模型加载 |
| FlagEmbedding | 1.3.3 | BGE-M3 Dense 编码 |
| sentence-transformers | 3.3.1 | Reranker（CrossEncoder） |
| rank-bm25 | 0.2.2 | BM25 稀疏检索 |
| jieba | 0.42.1 | 中文分词 |
| pydantic-settings | 2.15.0 | Settings 配置 |
| datasets / accelerate / peft | 5.0.1 / 1.15.0 / 0.12.0 | FlagEmbedding 依赖 |
| tokenizers | 0.20.3 | 分词器 |
| huggingface-hub | 0.24.0 | 模型下载 |
| Pillow | 最新 | sentence-transformers 依赖 |

> 版本兼容性说明：pip 默认拉取的最新版（transformers 5.x / FlagEmbedding 1.4.2 / huggingface-hub 1.x / peft 0.21）互相冲突，需锁定到上述稳定兼容组合（FlagEmbedding 1.3.3 使用标准 `torch_dtype` 参数，避免 1.4.2 的 `dtype` 参数与 transformers 4.x 不兼容）。
>
> ⚠️ 说明：上表为「期望/构建机实例化版本」。`requirement.txt` 当前**未精确 pin** 这些版本（V2 段仅有包名、且未显式列出 torch/transformers/sentence-transformers），与「依赖 freeze 未锁」这一遗留项一致。交付前建议补 pin（见第 6 节回滚点）。

### 2.2 新增模块（core/retrieval/）

| 文件 | 内容 |
|------|------|
| `core/retrieval/__init__.py` | 包声明 |
| `core/retrieval/dense.py` | `BGE3Embeddings`（BGE-M3 的 langchain 封装）+ `DenseRetriever` + `build_document_filter` + `to_evidence` |
| `core/retrieval/sparse.py` | `BM25Retriever`（内存 BM25，中英文分词） |
| `core/retrieval/fusion.py` | `reciprocal_rank_fusion`（RRF，c=60） |
| `core/retrieval/reranker.py` | `BGEReranker`（bge-reranker-v2-m3 CrossEncoder） |
| `core/retrieval/pipeline.py` | `RetrievalPipeline`（Dense+Sparse→RRF→Rerank 统一入口） |

### 2.3 索引重建脚本

| 文件 | 内容 |
|------|------|
| `scripts/reindex_v2.py` | 遍历 `documentStatus=2` 的 PDF，用新 `split_text` 分块 + BGE-M3 embedding 写入 V2 Chroma，并持久化 BM25 corpus |

关键行为：
- `chunk_id` 格式：`{documentID}:p{page}:c{idx}`
- metadata 统一：`chunk_id / documentID / knowledgeID / page_number / chunk_index / source / index_version`
- V2 索引目录：`CHROMA_LAYER1_V2_DIR = ./res/layer1_v2`（不覆盖旧 layer1）
- BM25 corpus：`BM25_INDEX_DIR = ./res/bm25/corpus.pkl`

---

## 三、检索参数（Settings）

| 参数 | 值 | 说明 |
|------|----|------|
| DENSE_K | 20 | Dense 候选召回数 |
| SPARSE_K | 20 | BM25 候选召回数 |
| RRF_C | 60 | 排名融合平滑常数 |
| RERANK_CANDIDATES | 24 | 送入 Reranker 的候选数 |
| FINAL_K | 8 | 最终返回证据数 |
| EVIDENCE_THRESHOLD | 0.65 | 证据充分度阈值（阶段三使用） |
| MAX_RETRIEVAL_ROUNDS | 2 | 最大扩检轮数（阶段三使用） |

---

## 四、验证结果

| 验证项 | 结果 |
|--------|------|
| BGE-M3 模型加载（本地缓存） | ✅ 1024 维，约 9.7s |
| bge-reranker-v2-m3 加载 | ✅ 约 8.3s |
| BM25 检索 | ✅ 通过（中英文分词） |
| RRF 融合 | ✅ 通过 |
| **Hybrid Pipeline 端到端** | ✅ 返回结构化证据（含 dense/sparse/fusion/rerank 分数） |
| V2 索引重建 | ✅ 1 文档 4 chunks 写入 layer1_v2 + BM25 corpus |

端到端检索示例（query=「这份文档的主要内容是什么」）：

```
返回 4 条证据:
  [1] page=2 sparse=0.49  fusion=0.0317 rerank=0.0231
  [2] page=3 sparse=1.394 fusion=0.0323 rerank=0.0069
  [3] page=1 sparse=2.499 fusion=0.0328 rerank=0.0021
  [4] page=4 sparse=0.324 fusion=0.0312 rerank=0.0006
```

每条证据均携带 `document_id / page_number / chunk_id / dense_score / sparse_score / fusion_score / rerank_score`，满足 Citation 与 Grounding 的可追溯要求。

---

## 五、遗留问题与待优化

1. **dense_score 语义**：当前存的是 Chroma 的 L2 距离（越小越相关），建议后续转为相似度语义（1 - 归一化距离）或改用 `similarity_search_with_relevance_scores`。不影响 RRF（RRF 只用排名）。
2. **FlagEmbedding 析构告警**：进程退出时 `AbsEmbedder.__del__` 会打印 `TypeError: 'NoneType' object is not callable`，是 FlagEmbedding 1.3.3 的已知清理问题，不影响检索结果。
3. **BM25 语料持久化**：首版用 pickle + 内存 BM25，符合文档「比赛规模」定位；后续可替换 Elasticsearch/OpenSearch。

---

## 六、回滚点

- 旧 ONNX embedding 与旧 Chroma `layer1` 完全保留，`RetrievalPipeline` 尚未接入 Router，现有 `/chat/*` 问答链路不受影响；
- V2 索引在独立目录 `layer1_v2`，可随时删除重建（`scripts/reindex_v2.py`）；
- 依赖版本已锁定，`requirement.txt` 记录了 V2 依赖（未 pin 精确版本，建议后续 freeze）。

---

## 七、下一步（阶段三）

1. 实现 `core/decision/`（DecisionEngine：Intent / Relatedness / Evidence 判断，Fail-closed）
2. 实现 `core/evidence/grounding.py`（Grounding Verify）
3. 新增 `router_qa.py`，把 `RetrievalPipeline` + `DecisionEngine` + `formatter` 接成 Ask Mode 可信问答闭环
4. 旧 `/chat/*` 作为兼容接口保留
