# PaperAgent V2 · 阶段五/六交付文档

> 阶段：阶段五（M4 Research Mode）+ 阶段六（M5 Skill Framework + Quantum 骨架）
> 完成日期：2026-09-24
> 验收结论：✅ 通过（Quantum 真实调用按约定暂缓，仅留接口骨架）

---

## 一、阶段目标

阶段五对应实施方案「Executable Research Agent」，把复杂科研目标拆解为可执行、可重试的任务步骤，最终交付结构化 Artifact；阶段六对应「Extensible Scientific Skill」，以统一 Skill 契约接入专业工具，Quantum 实验作为首个专业工具案例（本轮仅留接口骨架）。

---

## 二、完成的工作

### 2.1 Skill Framework（阶段六）

| 文件 | 内容 |
|------|------|
| `core/skills/base.py` | `BaseSkill`（抽象基类）+ `SkillResult`，统一生命周期：Schema 校验 → execute → 结果标准化 |
| `core/skills/registry.py` | `SkillRegistry`（注册/查重/描述/名称列表） |
| `core/skills/builtin.py` | `local_retrieval` / `paper_compare` / `report_generate` |
| `core/skills/paper_search.py` | `paper_search`（包装 arxiv_client） |
| `core/skills/paper_reader.py` | `paper_reader`（按 documentID/page 读 PDF） |
| `core/skills/extract.py` | `method_extract` / `experiment_extract`（LLM 结构化抽取） |
| `core/skills/citation_verify.py` | `citation_verify`（复用 GroundingVerifier） |

内置 Skills（共 8 个）：

| Skill | 依赖 | 功能 |
|-------|------|------|
| `local_retrieval` | RetrievalPipeline | 混合检索，返回 EvidenceChunk |
| `paper_search` | arxiv_client | 按关键词搜索 arXiv，返回结构化论文列表 |
| `paper_reader` | PyMuPDF + db | 按 documentID/page 读取论文原文 |
| `method_extract` | LLM | 抽取研究方法（MethodCard） |
| `experiment_extract` | LLM | 抽取实验配置（ExperimentConfig） |
| `paper_compare` | LLM | 跨论文比较，输出结构化对比表 |
| `citation_verify` | GroundingVerifier | 批量校验 claim-evidence |
| `report_generate` | LLM | 生成 Markdown 研究报告 |

### 2.2 Quantum 接口骨架（阶段六，按约定暂缓）

| 文件 | 内容 |
|------|------|
| `core/skills/quantum/schemas.py` | `QuantumExperimentInput` / `QuantumExperimentOutput` |
| `core/skills/quantum/adapter.py` | `QuantumToolAdapter` 骨架：白名单校验 + `run` 占位（真实接口待确认） |

> ⚠️ 事实约束：仅有「量子算法模拟性能优化工具软件 V1.0」软件著作权，未提供源代码/CLI/HTTP API。adapter 仅定义可实施接口，算法/参数白名单为空，未编造真实工具能力。

### 2.3 Research Mode（阶段五）

| 文件 | 内容 |
|------|------|
| `core/research/schemas.py` | `TaskPlan` / `PlanStep` / `StepResult` / `TaskState` |
| `core/research/planner.py` | `Planner`（LLM 生成计划 + 静态校验） |
| `core/research/executor.py` | `Executor`（顺序执行 + 依赖注入） |
| `core/research/verifier.py` | `Verifier`（成功条件检查） |
| `core/research/orchestrator.py` | `ResearchOrchestrator`（编排闭环） |

### 2.4 Artifact（阶段五）

| 文件 | 内容 |
|------|------|
| `core/artifact/models.py` | `Artifact` 模型（paper_list / comparison_table / research_report / evidence_table / experiment_report） |
| `core/artifact/builder.py` | 构建器（`build_comparison_table` / `build_research_report` 等） |

---

## 三、核心设计

### 3.1 Skill 生命周期（统一契约）

```
Schema 校验（input_model.model_validate）
  → execute（抽象方法，ctx 注入依赖）
  → 结果标准化（SkillResult：ok/output/evidence/artifacts/error）
  → 异常兜底（BaseSkill.run 捕获，返回 error_code=SKILL_ERROR）
```

### 3.2 Research 编排（Planner → Executor → Verifier）

```
Planner：目标 → LLM 生成 TaskPlan → 静态校验（skill 必须已注册、依赖必须存在）
Executor：顺序执行步骤，前置步骤输出注入 _previous_outputs
Verifier：检查所有步骤是否 SUCCESS
Orchestrator：串联三者，返回 TaskState（含 plan/step_results/artifacts）
```

---

## 四、验证结果

| 验证项 | 结果 |
|--------|------|
| SkillRegistry 注册 + 查重 | ✅ 8 个 Skill 注册成功 |
| Planner 生成计划 | ✅ `s1=local_retrieval → s2=report_generate` |
| Executor 顺序执行 | ✅ 两步均 SUCCESS |
| Orchestrator 编排 | ✅ 任务状态 SUCCESS |
| Artifact 传递 | ✅ 正确传递（修复后） |
| Quantum 白名单校验 | ✅ 未支持算法/参数抛出 `QuantumToolError` |

端到端验证（目标「检索 Day01 作业中 IP 地址分类内容并生成总结报告」）：

```
任务状态: SUCCESS
计划步骤: [('s1', 'local_retrieval'), ('s2', 'report_generate')]
步骤结果: [('s1', 'SUCCESS'), ('s2', 'SUCCESS')]
```

---

## 五、过程中的修复

1. **Planner task_id 缺失**：LLM 生成的 JSON 不含 `task_id`（required 字段），导致 `model_validate` 校验失败。修复为解析前先填充 `task_id`。
2. **Artifact 传递断裂**：`SkillResult.artifacts` 未传递到 `StepResult.output`，导致 Orchestrator 提取不到 artifact。修复为 executor 中把 `result.artifacts` 合并进 `output["artifacts"]`。

---

## 六、遗留问题与待优化

1. **Research 持久化未做**：`TaskState` 目前为内存态，刷新即丢失。需后续新增 `research_tasks` / `research_steps` / `skill_runs` / `artifacts` 表（阶段八）。
2. **Executor 仅顺序执行**：未实现互不依赖步骤的并行（文档标注首版可顺序）。
3. **Quantum 真实调用未实现**：白名单为空，待真实工具接口确认后补齐 adapter。
4. **FlagEmbedding 析构告警**：进程退出时打印 `TypeError: 'NoneType' object is not callable`（已知，不影响功能）。

---

## 七、回滚点

- 全部为新增模块，未修改既有 `/chat/*`、`/qa/stream` 等接口；
- Skill / Research / Artifact 相互独立，可单独调整；
- Quantum 骨架在工具不可用时不影响主系统。

---

## 八、下一步

1. **Research 任务持久化**（阶段八）：新增数据库表 + `router_research.py`。
2. **前端 Research Workspace**（阶段七）：ModeSwitch / PlanPanel / StepTimeline / ArtifactPanel。
3. **Quantum 真实接口**：拿到真实 CLI/API 后补 adapter + 白名单。
