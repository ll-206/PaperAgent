# PaperAgent V2 · 阶段八交付文档

> 阶段：阶段八（M4 补充）Research 任务持久化
> 完成日期：2026-09-24
> 验收结论：✅ 通过（核心持久化链路闭环；Alembic 迁移与部分表待后续）

---

## 一、阶段目标

把阶段五实现的 Research Mode 从「内存态」升级为「可持久化」：任务（task）、步骤（step）、工具调用审计（skill_run）、交付物（artifact）落库，刷新页面后状态仍可查询，为前端 Research Workspace 与实验二（Task Completion）提供数据底座。

---

## 二、完成的工作

### 2.1 数据库表（models.py）

| 表 | 模型 | 关键字段 |
|----|------|---------|
| `research_tasks` | `ResearchTask` | task_id, lid, goal, mode, status, plan_json, created_at, updated_at |
| `research_steps` | `ResearchStep` | step_id, task_id, skill_name, status, input_json, output_json, error, duration_ms |
| `skill_runs` | `SkillRun` | run_id, task_id, step_id, duration_ms, status, error, trace_json |
| `artifacts` | `Artifact` | artifact_id, task_id, type, title, data_json |

### 2.2 API 路由（router_research.py）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/research/tasks` | 创建任务（Planner 生成计划 → Executor 执行 → 持久化 task/steps/artifacts） |
| GET | `/research/tasks` | 当前用户任务列表 |
| GET | `/research/tasks/{task_id}` | 任务详情（plan + steps + artifacts） |

### 2.3 应用装配（main.py）

- `_build_skill_registry()`：注册 8 个内置 Skills
- `_build_research_orchestrator()`：构建 Planner → Executor → Verifier → Orchestrator
- lifespan 初始化 `app.skill_registry` 与 `app.research_orchestrator`（注入 RetrievalPipeline / LLM / DecisionEngine / db）

---

## 三、核心设计

### 3.1 同步用户校验

`router_research` 端点保持 `sync def`（因 `ResearchOrchestrator.run` 内部 `asyncio.run`，不能在事件循环内调用），故新增 `_get_user(token, db)` 同步解析 JWT + `query_user`，替代 async 的 `get_current_user` 依赖注入。

### 3.2 持久化流程

```
POST /research/tasks
  → orchestrator.run(goal)            # Planner + Executor + Verifier
  → 写入 research_tasks（plan_json）
  → 写入 research_steps（每个 step）
  → 写入 artifacts（每个 artifact）
  → commit
```

---

## 四、验证结果

| 验证项 | 结果 |
|--------|------|
| 4 张新表创建 | ✅ research_tasks / research_steps / skill_runs / artifacts |
| 后端启动 + Research Orchestrator（8 Skills） | ✅ 初始化完成 |
| POST /research/tasks 创建+执行 | ✅ HTTP 200 |
| GET /research/tasks 列表 | ✅ 正确返回 |
| GET /research/tasks/{task_id} 详情 | ✅ 返回 plan + steps + artifacts |

端到端任务（目标「检索 IP 地址分类并生成总结」）：

```
状态: FAILED
步骤: local_retrieval(SUCCESS) → citation_verify(FAILED) → report_generate(SUCCESS)
artifact: research_report（1 个）
```

---

## 五、过程中的修复

1. **async 依赖注入错误**：`get_current_user` 是 async 函数，在 sync 端点直接调用得到 coroutine，报 `'coroutine' object has no attribute 'lid'`。修复为新增同步 `_get_user` 函数。

---

## 六、遗留问题与待优化

1. **未引入 Alembic**：仍依赖 `Base.metadata.create_all` 建表。文档要求引入 Alembic 做版本化迁移，首版为控制范围暂缓。
2. **Planner 计划质量问题**：本次生成含 `citation_verify` 步骤，但其 `input`（answer/evidence）为空导致验证失败。需优化 Planner prompt，精确设置步骤依赖与输入。
3. **skill_runs 表未写入**：Executor 尚未记录每次 Skill 调用的审计信息（run_id / duration_ms / trace_json），表结构已就绪。
4. **evidence_records / chat_sessions 表未建**：文档 6.8 提到但本轮未实现，证据追踪与会话持久化待后续。

---

## 七、回滚点

- 新增表通过 `create_all` 自动创建，不影响既有表结构；
- `router_research` 为新增路由，旧 `/chat/*`、`/qa/stream` 不受影响；
- Research 持久化可独立调整，不侵入 Ask Mode 链路。

---

## 八、下一步

1. **Alembic 迁移**：接入 `alembic init migrations`，生成 001_research_tables。
2. **Planner 优化**：改进 PLANNER_PROMPT，约束步骤依赖与输入。
3. **SkillRun 记录**：Executor 每次执行 Skill 写入 skill_runs。
4. **前端 Research Workspace**（阶段七）：ResearchHistory 读取 `/research/tasks`，PlanPanel/StepTimeline/ArtifactPanel 展示。
